#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include "myTypes.c"

int main(){
  
  printf("sizeof(size_t) = %d\n", sizeof(size_t));
  printf("sizeof(uint) = %d\n", sizeof(uint));

  return  0;
}
